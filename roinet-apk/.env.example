# API base URL — use 10.0.2.2:8000 for Android emulator, localhost for iOS simulator
EXPO_PUBLIC_API_URL=http://localhost:8000

# Set to true for offline demo with seeded CRM data (no backend required)
EXPO_PUBLIC_USE_MOCK=true
